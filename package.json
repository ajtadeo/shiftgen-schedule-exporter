{
  "type": "module",
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-chrome": "^0.8.0",
    "jest-environment-jsdom": "^30.3.0"
  },
  "overrides": {
    "jest-chrome": {
      "jest": "$jest"
    }
  },
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  }
}
